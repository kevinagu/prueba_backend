<%= form_for @photo_user, :html => {:multipart => true} do |f| %>
  <% if @photo_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_user.errors.count, "error") %> prohibited this photo_user from being saved:</h2>

      <ul>
      <% @photo_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.file_field :photo %>
  </div>
  <div class="actions">
    <%= f.button  :Guardar , name: "form",value: "Actualizar"%>

  </div>
<% end %>
